Добрый день! 

Используемая литература
cppreference
и картинка которую прилагаю к проекту.

Имеется три потока
1) поток отрисовки
2) поток расчетов точки и скоростей 
3) поток "инициирования" взрывов 

Поток отрисовки только рисует и ничего больше
Поток расчетов высчитывает новые точки и скорости
Поток взрывов гасит и порождает новые ноды точек

В качестве хранилища точек был выбран вектор структур, так как хотелось иметь атомарность координат точек и не атомарный флаг определяющий жива или не жива точка. 
Для этого были переопределены конструктор копирования и оператор копирующего присваивания.

Взрывы синхронизировались при помощи атомарного флага.

В решении имеются два серьезных недостатка:
1)я его не успела доделать так как это вынуждает перепиливать частично логику, но в рамках личного интереса я это переделаю:
Положение откуда можно начинать отрисовку координируется обычной статической переменной, если если сделать несколько кликов до истечения 1.5 секунд то взрыв произойдет лишь на последней. 
2) высчет рандомного положения для взрыва - это просто выглядит криво 
Менее серьезный но явный недостаток:
1) паттерн взрыва, я сделала его одинаковым .



В перспективе я бы доработала это решение , главные пункты которые подверглись бы переделке:
1) хранилище точек: 
-я бы сделала хранилище деревом поверх вектора
или
List хранящий адреса, так как можно атомарно их подменять имитируя появление новых точек и уничтожение старых
2) Переработать генерацию рандомного положения, в нынешней реализации мне пока сложно предположить решение,надо подумать,
 так как в случае если я инициирую кликом взрыв вблизи границы "жизни" точек я получу кучу "мертвых" точек моментально
3) переработать логику со статической переменной определяющей ноду которая должна взорвать. 
Первый вариант который я думаю попробовать это пулять в тред переменную определяющую взрыв и скореектировать в соотвествии логику в треде взрывов


Заранее спасибо за вашу критику и за весьма интересную задачу)
